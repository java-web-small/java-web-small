image: openjdk:17-alpine

cache:
  paths:
    - .m2/repository

stages:
  - syntax-check
  
syntax-check:
  stage: syntax-check
  script:
    - echo "🔁 Build and check syntax..."
    - ./mvnw verify -DskipTests
  rules:
    - if: $CI_COMMIT_BRANCH
      allow_failure: false
